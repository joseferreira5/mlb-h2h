import styled from 'styled-components';

const Form = styled.form`
  grid-column: 1 / 4;
  align-self: center;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-around;
  align-items: center;
  height: 100%;
  padding: 0 1em;
`;

export default Form;
