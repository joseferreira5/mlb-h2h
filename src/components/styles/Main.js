import styled from 'styled-components';

const Main = styled.main`
  max-width: 900px;
  margin: 0 auto;
  min-height: calc(100% - 5em);
  height: calc(100% - 5em);
  padding: 1.2em 0;
`;

export default Main;
